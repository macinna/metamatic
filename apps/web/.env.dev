# Web/Frontend development environment configuration
# Overrides configs/.env.dev for web frontend

# API endpoints
NEXT_PUBLIC_API_URL=https://api.dev.metamatic.app

# Strava OAuth (public client ID is safe to include)
NEXT_PUBLIC_STRAVA_CLIENT_ID=your_dev_strava_client_id

# Environment settings
NEXT_PUBLIC_ENVIRONMENT=dev

# Development settings
NEXT_PUBLIC_DEBUG=false
